[project]
name = "mini_actions_demo"
version = "0.1.0"
description = "Small sample project to test GitHub Actions"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
dev = ["pytest"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
